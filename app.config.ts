/**
 * App identity. app-builder fills businessName from the attendee's answer
 * during scaffolding — it is the page heading and browser-tab title.
 */
export const appConfig = {
  businessName: "Your Business",
};
